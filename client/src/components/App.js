import { BrowserRouter } from "react-router-dom";
import Header from "./Header";

export const App = () => {
    return (
        <BrowserRouter>
             <Header />
        </BrowserRouter>

    )
}
